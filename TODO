To do before next major release (0.8.0):

Change NickServ plugin into Services plugin.
Fix Quotes plugin.
Fix (or nuke :)) Factoids plugin.
Fix Infobot plugin.
Fix website.
Write some more tests (Alias module, etc.)

To do later:

Fix Enforcer plugin.
